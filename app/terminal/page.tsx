import Terminal from "@/components/terminal/Terminal";

export const metadata = {
  title: "The Terminal - Web Redesigned",
  description: "CLI-style interface for exploring the portfolio",
};

export default function TerminalPage() {
  return <Terminal />;
}
